#include <stdio.h>
/**
 * main - prints the phrace with proper grammer
 * but the outcome is a piece of art
 * Return: o if excecuted properly non-zero otherwise
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}

